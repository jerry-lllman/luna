import Layouts from '@/layouts'

export default function Home() {
  // const [list, setList] = useState()

  // const list = new Array(20).fill(1).map((_, index) => index)

  return (
    <Layouts showHeader title="Luna">
      <div>1111</div>
    </Layouts>
  )
}
